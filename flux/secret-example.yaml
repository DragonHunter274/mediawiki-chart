# Example: create this secret manually or via sealed-secrets / SOPS before deploying.
# Do NOT commit real credentials to git.
#
#   kubectl create secret generic mediawiki-db-credentials \
#     --namespace mediawiki \
#     --from-literal=rootPassword='your-root-password' \
#     --from-literal=password='your-user-password'
#
apiVersion: v1
kind: Secret
metadata:
  name: mediawiki-db-credentials
  namespace: mediawiki
type: Opaque
stringData:
  rootPassword: changeme
  password: changeme
